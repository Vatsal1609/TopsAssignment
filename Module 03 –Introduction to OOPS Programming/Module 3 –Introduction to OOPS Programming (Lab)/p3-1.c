#include <stdio.h>
main()
{
	int l,w;
	printf("Enter the length of Rectangle :");
	scanf("%d", &l);
	
	printf("Enter the Width of Rectangle :");
	scanf("%d", &w);
	
	printf("\n Area of rectangle is %d", l*w);
		
}
